// users table
select * from users;
desc users;
// otps table
select * from otps;
desc otps;
// certificates table
select * from certificates;
desc certificates;


// delete user by id
DELETE FROM users WHERE id = ?;

// update user details by id
UPDATE users SET roll_number = ?, phone_number = ? WHERE id = ?;

// insert user in users table
INSERT INTO users (full_name, roll_number, course, branch, phone_number, email_id, username, password)
VALUES (?, ?, ?, ?, ?, ?, ?, ?);

// Alter table / Add column 
ALTER TABLE users ADD COLUMN ? VARCHAR(100) NULL AFTER ?;
