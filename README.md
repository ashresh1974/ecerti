<<<<<<< HEAD
# e-Certificate Management System

This project is an e-certificate management system for an engineering college. It consists of a React.js frontend and a Node.js backend with a MySQL database.

## Project Structure

- `frontend/`: React.js application for the user interface.
- `backend/`: Node.js application for the API and business logic.

## Setup and Installation

### Prerequisites

- Node.js (LTS version recommended)
- npm (comes with Node.js)
- MySQL Server

### Backend Setup

1. Navigate to the `backend` directory:
   ```bash
   cd backend
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Create a MySQL database named `ecertificate_db`.
4. Update the database connection details in `backend/src/db.js` and `backend/src/server.js` if necessary.
5. Start the backend server:
   ```bash
   npm start
   ```
   (Alternatively, you can run the VS Code task "Start Backend")

### Frontend Setup

1. Navigate to the `frontend` directory:
   ```bash
   cd frontend
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Start the React development server:
   ```bash
   npm start
   ```
   (Alternatively, you can run the VS Code task "Start Frontend")

## Usage

Once both the frontend and backend servers are running, you can access the application in your web browser at `http://localhost:3000` (or the port specified by the React app).

## Features (Planned)

- User authentication and authorization
- Certificate generation and management
- Certificate distribution (email, download)
- Admin dashboard for managing users and certificates
- Student portal for viewing and downloading certificates

## Contributing

Feel free to contribute to this project. Please follow standard GitHub flow for contributions.

## License

[Specify your license here, e.g., MIT License]
=======
# ecertificate
ecertificate management 
>>>>>>> 0dd2674838f46b9e5fcb921f417fdad579173de3
